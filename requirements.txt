# التطبيق يعمل بمكتبات بايثون القياسية فقط.
# الحزم التالية اختيارية وتحسّن التجربة:

# نافذة سطح مكتب أصلية على ويندوز (WebView2/Edge) بدل فتح المتصفح
pywebview>=5.0 ; sys_platform == "win32"

# لتوليد ملف EXE واحد (أدوات تطوير فقط)
pyinstaller>=6.0
